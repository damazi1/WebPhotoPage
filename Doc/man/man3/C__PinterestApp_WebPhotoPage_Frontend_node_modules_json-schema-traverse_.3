.TH "Dokumentacja katalogu C:/PinterestApp/WebPhotoPage/Frontend/node_modules/json-schema-traverse" 3 "PinterestApp" \" -*- nroff -*-
.ad l
.nh
.SH NAME
Dokumentacja katalogu C:/PinterestApp/WebPhotoPage/Frontend/node_modules/json-schema-traverse
.SH SYNOPSIS
.br
.PP
.SH "Opis szczegółowy"
.PP 

.SH "json-schema-traverse"
.PP
Traverse JSON Schema passing each schema object to callback

.PP
\fR\fP \fR\fP \fR\fP
.SH "Install"
.PP
.PP
.nf
npm install json\-schema\-traverse
.fi
.PP
.SH "Usage"
.PP
.PP
.nf
const traverse = require('json\-schema\-traverse');
const schema = {
  properties: {
    foo: {type: 'string'},
    bar: {type: 'integer'}
  }
};

traverse(schema, {cb});
// cb is called 3 times with:
// 1\&. root schema
// 2\&. {type: 'string'}
// 3\&. {type: 'integer'}

// Or:

traverse(schema, {cb: {pre, post}});
// pre is called 3 times with:
// 1\&. root schema
// 2\&. {type: 'string'}
// 3\&. {type: 'integer'}
//
// post is called 3 times with:
// 1\&. {type: 'string'}
// 2\&. {type: 'integer'}
// 3\&. root schema
.fi
.PP

.PP
Callback function \fRcb\fP is called for each schema object (not including draft-06 boolean schemas), including the root schema, in pre-order traversal\&. Schema references ($ref) are not resolved, they are passed as is\&. Alternatively, you can pass a \fR{pre, post}\fP object as \fRcb\fP, and then \fRpre\fP will be called before traversing child elements, and \fRpost\fP will be called after all child elements have been traversed\&.

.PP
Callback is passed these parameters:

.PP
.IP "\(bu" 2
\fIschema\fP: the current schema object
.IP "\(bu" 2
\fIJSON pointer\fP: from the root schema to the current schema object
.IP "\(bu" 2
\fIroot schema\fP: the schema passed to \fRtraverse\fP object
.IP "\(bu" 2
\fIparent JSON pointer\fP: from the root schema to the parent schema object (see below)
.IP "\(bu" 2
\fIparent keyword\fP: the keyword inside which this schema appears (e\&.g\&. \fRproperties\fP, \fRanyOf\fP, etc\&.)
.IP "\(bu" 2
\fIparent schema\fP: not necessarily parent object/array; in the example above the parent schema for \fR{type: 'string'}\fP is the root schema
.IP "\(bu" 2
\fIindex/property\fP: index or property name in the array/object containing multiple schemas; in the example above for \fR{type: 'string'}\fP the property name is \fR'foo'\fP
.PP
.SH "Traverse objects in all unknown keywords"
.PP
.PP
.nf
const traverse = require('json\-schema\-traverse');
const schema = {
  mySchema: {
    minimum: 1,
    maximum: 2
  }
};

traverse(schema, {allKeys: true, cb});
// cb is called 2 times with:
// 1\&. root schema
// 2\&. mySchema
.fi
.PP

.PP
Without option \fRallKeys: true\fP callback will be called only with root schema\&.
.SH "License"
.PP
\fRMIT\fP 
