.TH "Dokumentacja katalogu C:/PinterestApp/WebPhotoPage/Frontend/node_modules/axios/lib/adapters" 3 "PinterestApp" \" -*- nroff -*-
.ad l
.nh
.SH NAME
Dokumentacja katalogu C:/PinterestApp/WebPhotoPage/Frontend/node_modules/axios/lib/adapters
.SH SYNOPSIS
.br
.PP
.SH "Opis szczegółowy"
.PP 

.SH "axios // adapters"
.PP
The modules under \fRadapters/\fP are modules that handle dispatching a request and settling a returned \fRPromise\fP once a response is received\&.
.SH "Example"
.PP
.PP
.nf
var settle = require('\&./\&.\&./core/settle');

module\&.exports = function myAdapter(config) {
  // At this point:
  //  \- config has been merged with defaults
  //  \- request transformers have already run
  //  \- request interceptors have already run
  
  // Make the request using config provided
  // Upon response settle the Promise

  return new Promise(function(resolve, reject) {
  
    var response = {
      data: responseData,
      status: request\&.status,
      statusText: request\&.statusText,
      headers: responseHeaders,
      config: config,
      request: request
    };

    settle(resolve, reject, response);

    // From here:
    //  \- response transformers will run
    //  \- response interceptors will run
  });
}
.fi
.PP
 
