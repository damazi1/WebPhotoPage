.TH "Dokumentacja katalogu C:/PinterestApp/WebPhotoPage/Frontend/node_modules/optionator" 3 "PinterestApp" \" -*- nroff -*-
.ad l
.nh
.SH NAME
Dokumentacja katalogu C:/PinterestApp/WebPhotoPage/Frontend/node_modules/optionator
.SH SYNOPSIS
.br
.PP
.SH "Opis szczegółowy"
.PP 

.SH "Optionator"
.PP

.PP
Optionator is a JavaScript/Node\&.js option parsing and help generation library used by \fReslint\fP, \fRGrasp\fP, \fRLiveScript\fP, \fResmangle\fP, \fRescodegen\fP, and \fRmany more\fP\&.

.PP
For an online demo, check out the \fRGrasp online demo\fP\&.

.PP
\fBAbout\fP  \fBUsage\fP  \fBSettings Format\fP  \fBArgument Format\fP
.SH "Why?"
.PP
The problem with other option parsers, such as \fRyargs\fP or \fRminimist\fP, is they just accept all input, valid or not\&. With Optionator, if you mistype an option, it will give you an error (with a suggestion for what you meant)\&. If you give the wrong type of argument for an option, it will give you an error rather than supplying the wrong input to your application\&. 
.PP
.nf
$ cmd --halp
Invalid option '--halp' - perhaps you meant '--help'?

$ cmd --count str
Invalid value for option 'count' - expected type Int, received value: str\&.

.fi
.PP

.PP
Other helpful features include reformatting the help text based on the size of the console, so that it fits even if the console is narrow, and accepting not just an array (eg\&. process\&.argv), but a string or object as well, making things like testing much easier\&.
.SH "About"
.PP
Optionator uses \fRtype-check\fP and \fRlevn\fP behind the scenes to cast and verify input according the specified types\&.

.PP
MIT license\&. Version 0\&.9\&.4 
.PP
.nf
npm install optionator

.fi
.PP

.PP
For updates on Optionator, \fRfollow me on twitter\fP\&.

.PP
Optionator is a Node\&.js module, but can be used in the browser as well if packed with webpack/browserify\&.
.SH "Usage"
.PP
\fRrequire('optionator');\fP returns a function\&. It has one property, \fRVERSION\fP, the current version of the library as a string\&. This function is called with an object specifying your options and other information, see the \fBsettings format section\fP\&. This in turn returns an object with three properties, \fRparse\fP, \fRparseArgv\fP, \fRgenerateHelp\fP, and \fRgenerateHelpForOption\fP, which are all functions\&.

.PP
.PP
.nf
var optionator = require('optionator')({
    prepend: 'Usage: cmd [options]',
    append: 'Version 1\&.0\&.0',
    options: [{
        option: 'help',
        alias: 'h',
        type: 'Boolean',
        description: 'displays help'
    }, {
        option: 'count',
        alias: 'c',
        type: 'Int',
        description: 'number of things',
        example: 'cmd \-\-count 2'
    }]
});

var options = optionator\&.parseArgv(process\&.argv);
if (options\&.help) {
    console\&.log(optionator\&.generateHelp());
}
\&.\&.\&.
.fi
.PP
.SS "parse(input, parseOptions)"
\fRparse\fP processes the \fRinput\fP according to your settings, and returns an object with the results\&.
.SS "arguments"
.IP "\(bu" 2
input - \fR[String] | Object | String\fP - the input you wish to parse
.IP "\(bu" 2
parseOptions - \fR{slice: Int}\fP - all options optional
.IP "  \(bu" 4
\fRslice\fP specifies how much to slice away from the beginning if the input is an array or string - by default \fR0\fP for string, \fR2\fP for array (works with \fRprocess\&.argv\fP)
.PP

.PP
.SS "returns"
\fRObject\fP - the parsed options, each key is a camelCase version of the option name (specified in dash-case), and each value is the processed value for that option\&. Positional values are in an array under the \fR_\fP key\&.
.SS "example"
.PP
.nf
parse(['node', 't\&.js', '\-\-count', '2', 'positional']); // {count: 2, _: ['positional']}
parse('\-\-count 2 positional');                         // {count: 2, _: ['positional']}
parse({count: 2, _:['positional']});                   // {count: 2, _: ['positional']}
.fi
.PP
.SS "parseArgv(input)"
\fRparseArgv\fP works exactly like \fRparse\fP, but only for array input and it slices off the first two elements\&.
.SS "arguments"
.IP "\(bu" 2
input - \fR[String]\fP - the input you wish to parse
.PP
.SS "returns"
See "returns" section in "parse"
.SS "example"
.PP
.nf
parseArgv(process\&.argv);
.fi
.PP
.SS "generateHelp(helpOptions)"
\fRgenerateHelp\fP produces help text based on your settings\&.
.SS "arguments"
.IP "\(bu" 2
helpOptions - \fR{showHidden: Boolean, interpolate: Object}\fP - all options optional
.IP "  \(bu" 4
\fRshowHidden\fP specifies whether to show options with \fRhidden: true\fP specified, by default it is \fRfalse\fP
.IP "  \(bu" 4
\fRinterpolate\fP specify data to be interpolated in \fRprepend\fP and \fRappend\fP text, \fR{{key}}\fP is the format - eg\&. \fRgenerateHelp({interpolate:{version: '0\&.4\&.2'}})\fP, will change this \fRappend\fP text: \fRVersion {{version}}\fP to \fRVersion 0\&.4\&.2\fP
.PP

.PP
.SS "returns"
\fRString\fP - the generated help text
.SS "example"
.PP
.nf
generateHelp(); /*
"Usage: cmd [options] positional

  \-h, \-\-help       displays help
  \-c, \-\-count Int  number of things

Version  1\&.0\&.0
"*/
.fi
.PP
.SS "generateHelpForOption(optionName)"
\fRgenerateHelpForOption\fP produces expanded help text for the specified with \fRoptionName\fP option\&. If an \fRexample\fP was specified for the option, it will be displayed, and if a \fRlongDescription\fP was specified, it will display that instead of the \fRdescription\fP\&.
.SS "arguments"
.IP "\(bu" 2
optionName - \fRString\fP - the name of the option to display
.PP
.SS "returns"
\fRString\fP - the generated help text for the option
.SS "example"
.PP
.nf
generateHelpForOption('count'); /*
"\-c, \-\-count Int
description: number of things
example: cmd \-\-count 2
"*/
.fi
.PP
.SH "Settings Format"
.PP
When your \fRrequire('optionator')\fP, you get a function that takes in a settings object\&. This object has the type: 
.PP
.nf
{
  prepend: String,
  append: String,
  options: [{heading: String} | {
    option: String,
    alias: [String] | String,
    type: String,
    enum: [String],
    default: String,
    restPositional: Boolean,
    required: Boolean,
    overrideRequired: Boolean,
    dependsOn: [String] | String,
    concatRepeatedArrays: Boolean | (Boolean, Object),
    mergeRepeatedObjects: Boolean,
    description: String,
    longDescription: String,
    example: [String] | String
  }],
  helpStyle: {
    aliasSeparator: String,
    typeSeparator: String,
    descriptionSeparator: String,
    initialIndent: Int,
    secondaryIndent: Int,
    maxPadFactor: Number
  },
  mutuallyExclusive: [[String | [String]]],
  concatRepeatedArrays: Boolean | (Boolean, Object), // deprecated, set in defaults object
  mergeRepeatedObjects: Boolean, // deprecated, set in defaults object
  positionalAnywhere: Boolean,
  typeAliases: Object,
  defaults: Object
}

.fi
.PP

.PP
All of the properties are optional (the \fRMaybe\fP has been excluded for brevities sake), except for having either \fRheading: String\fP or \fRoption: String\fP in each object in the \fRoptions\fP array\&.
.SS "Top Level Properties"
.IP "\(bu" 2
\fRprepend\fP is an optional string to be placed before the options in the help text
.IP "\(bu" 2
\fRappend\fP is an optional string to be placed after the options in the help text
.IP "\(bu" 2
\fRoptions\fP is a required array specifying your options and headings, the options and headings will be displayed in the order specified
.IP "\(bu" 2
\fRhelpStyle\fP is an optional object which enables you to change the default appearance of some aspects of the help text
.IP "\(bu" 2
\fRmutuallyExclusive\fP is an optional array of arrays of either strings or arrays of strings\&. The top level array is a list of rules, each rule is a list of elements - each element can be either a string (the name of an option), or a list of strings (a group of option names) - there will be an error if more than one element is present
.IP "\(bu" 2
\fRconcatRepeatedArrays\fP see description under the "Option Properties" heading - use at the top level is deprecated, if you want to set this for all options, use the \fRdefaults\fP property
.IP "\(bu" 2
\fRmergeRepeatedObjects\fP see description under the "Option Properties" heading - use at the top level is deprecated, if you want to set this for all options, use the \fRdefaults\fP property
.IP "\(bu" 2
\fRpositionalAnywhere\fP is an optional boolean (defaults to \fRtrue\fP) - when \fRtrue\fP it allows positional arguments anywhere, when \fRfalse\fP, all arguments after the first positional one are taken to be positional as well, even if they look like a flag\&. For example, with \fRpositionalAnywhere: false\fP, the arguments \fR--flag --boom 12 --crack\fP would have two positional arguments: \fR12\fP and \fR--crack\fP
.IP "\(bu" 2
\fRtypeAliases\fP is an optional object, it allows you to set aliases for types, eg\&. \fR{Path: 'String'}\fP would allow you to use the type \fRPath\fP as an alias for the type \fRString\fP
.IP "\(bu" 2
\fRdefaults\fP is an optional object following the option properties format, which specifies default values for all options\&. A default will be overridden if manually set\&. For example, you can do \fRdefault: { type: "String" }\fP to set the default type of all options to \fRString\fP, and then override that default in an individual option by setting the \fRtype\fP property
.PP
.SS "Heading Properties"
.IP "\(bu" 2
\fRheading\fP a required string, the name of the heading
.PP
.SS "Option Properties"
.IP "\(bu" 2
\fRoption\fP the required name of the option - use dash-case, without the leading dashes
.IP "\(bu" 2
\fRalias\fP is an optional string or array of strings which specify any aliases for the option
.IP "\(bu" 2
\fRtype\fP is a required string in the \fRtype check\fP \fRformat\fP, this will be used to cast the inputted value and validate it
.IP "\(bu" 2
\fRenum\fP is an optional array of strings, each string will be parsed by \fRlevn\fP - the argument value must be one of the resulting values - each potential value must validate against the specified \fRtype\fP
.IP "\(bu" 2
\fRdefault\fP is a optional string, which will be parsed by \fRlevn\fP and used as the default value if none is set - the value must validate against the specified \fRtype\fP
.IP "\(bu" 2
\fRrestPositional\fP is an optional boolean - if set to \fRtrue\fP, everything after the option will be taken to be a positional argument, even if it looks like a named argument
.IP "\(bu" 2
\fRrequired\fP is an optional boolean - if set to \fRtrue\fP, the option parsing will fail if the option is not defined
.IP "\(bu" 2
\fRoverrideRequired\fP is a optional boolean - if set to \fRtrue\fP and the option is used, and there is another option which is required but not set, it will override the need for the required option and there will be no error - this is useful if you have required options and want to use \fR--help\fP or \fR--version\fP flags
.IP "\(bu" 2
\fRconcatRepeatedArrays\fP is an optional boolean or tuple with boolean and options object (defaults to \fRfalse\fP) - when set to \fRtrue\fP and an option contains an array value and is repeated, the subsequent values for the flag will be appended rather than overwriting the original value - eg\&. option \fRg\fP of type \fR[String]\fP: \fR-g a -g b -g c,d\fP will result in \fR['a','b','c','d']\fP

.PP
You can supply an options object by giving the following value: \fR[true, options]\fP\&. The one currently supported option is \fRoneValuePerFlag\fP, this only allows one array value per flag\&. This is useful if your potential values contain a comma\&.
.IP "\(bu" 2
\fRmergeRepeatedObjects\fP is an optional boolean (defaults to \fRfalse\fP) - when set to \fRtrue\fP and an option contains an object value and is repeated, the subsequent values for the flag will be merged rather than overwriting the original value - eg\&. option \fRg\fP of type \fRObject\fP: \fR-g a:1 -g b:2 -g c:3,d:4\fP will result in \fR{a: 1, b: 2, c: 3, d: 4}\fP
.IP "\(bu" 2
\fRdependsOn\fP is an optional string or array of strings - if simply a string (the name of another option), it will make sure that that other option is set, if an array of strings, depending on whether \fR'and'\fP or \fR'or'\fP is first, it will either check whether all (\fR['and', 'option-a', 'option-b']\fP), or at least one (\fR['or', 'option-a', 'option-b']\fP) other options are set
.IP "\(bu" 2
\fRdescription\fP is an optional string, which will be displayed next to the option in the help text
.IP "\(bu" 2
\fRlongDescription\fP is an optional string, it will be displayed instead of the \fRdescription\fP when \fRgenerateHelpForOption\fP is used
.IP "\(bu" 2
\fRexample\fP is an optional string or array of strings with example(s) for the option - these will be displayed when \fRgenerateHelpForOption\fP is used
.PP
.SS "Help Style Properties"
.IP "\(bu" 2
\fRaliasSeparator\fP is an optional string, separates multiple names from each other - default: ' ,'
.IP "\(bu" 2
\fRtypeSeparator\fP is an optional string, separates the type from the names - default: ' '
.IP "\(bu" 2
\fRdescriptionSeparator\fP is an optional string , separates the description from the padded name and type - default: ' '
.IP "\(bu" 2
\fRinitialIndent\fP is an optional int - the amount of indent for options - default: 2
.IP "\(bu" 2
\fRsecondaryIndent\fP is an optional int - the amount of indent if wrapped fully (in addition to the initial indent) - default: 4
.IP "\(bu" 2
\fRmaxPadFactor\fP is an optional number - affects the default level of padding for the names/type, it is multiplied by the average of the length of the names/type - default: 1\&.5
.PP
.SH "Argument Format"
.PP
At the highest level there are two types of arguments: named, and positional\&.

.PP
Name arguments of any length are prefixed with \fR--\fP (eg\&. \fR--go\fP), and those of one character may be prefixed with either \fR--\fP or \fR-\fP (eg\&. \fR-g\fP)\&.

.PP
There are two types of named arguments: boolean flags (eg\&. \fR--problemo\fP, \fR-p\fP) which take no value and result in a \fRtrue\fP if they are present, the falsey \fRundefined\fP if they are not present, or \fRfalse\fP if present and explicitly prefixed with \fRno\fP (eg\&. \fR--no-problemo\fP)\&. Named arguments with values (eg\&. \fR--tseries 800\fP, \fR-t 800\fP) are the other type\&. If the option has a type \fRBoolean\fP it will automatically be made into a boolean flag\&. Any other type results in a named argument that takes a value\&.

.PP
For more information about how to properly set types to get the value you want, take a look at the \fRtype check\fP and \fRlevn\fP pages\&.

.PP
You can group single character arguments that use a single \fR-\fP, however all except the last must be boolean flags (which take no value)\&. The last may be a boolean flag, or an argument which takes a value - eg\&. \fR-ba 2\fP is equivalent to \fR-b -a 2\fP\&.

.PP
Positional arguments are all those values which do not fall under the above - they can be anywhere, not just at the end\&. For example, in \fRcmd -b one -a 2 two\fP where \fRb\fP is a boolean flag, and \fRa\fP has the type \fRNumber\fP, there are two positional arguments, \fRone\fP and \fRtwo\fP\&.

.PP
Everything after an \fR--\fP is positional, even if it looks like a named argument\&.

.PP
You may optionally use \fR=\fP to separate option names from values, for example: \fR--count=2\fP\&.

.PP
If you specify the option \fRNUM\fP, then any argument using a single \fR-\fP followed by a number will be valid and will set the value of \fRNUM\fP\&. Eg\&. \fR-2\fP will be parsed into \fRNUM: 2\fP\&.

.PP
If duplicate named arguments are present, the last one will be taken\&.
.SH "Technical About"
.PP
\fRoptionator\fP is written in \fRLiveScript\fP - a language that compiles to JavaScript\&. It uses \fRlevn\fP to cast arguments to their specified type, and uses \fRtype-check\fP to validate values\&. It also uses the \fRprelude\&.ls\fP library\&. 
